# Cinder — audit, forward plan, and the skins design system

**Written 2026-09-14, against `7860396` (v0.3.4).** Supersedes nothing; it sits alongside
[`SHORTCOMINGS.md`](SHORTCOMINGS.md) (standing structural reference) and
[`DEVICE_CHECKLIST.md`](DEVICE_CHECKLIST.md) (the device run sheet), and it **revises**
[`PLAN_skins.md`](PLAN_skins.md) — Part C below replaces that file's step 2 with a corrected
contract, for a reason Part C evidences rather than asserts.

Every number here was measured on this tree today. Where something is inference, it says so.

---

## Part A — What the tree actually is, today

All offline gates were run on `7860396`, not quoted from a previous audit:

| Gate | Command | Result |
|---|---|---|
| Rust tests | `cargo test --release` | **549 pass, 0 fail** (406 `cinder-ui`, 107 `cinder-ffi`, 23 db, 8 installer, 3 host, 2 sim) |
| Off-device harness | `cinder-home/harness/run.sh` | **41 scenarios pass** |
| C/C++ syntax | `tools/host_syntax_check.sh` | 23 files, all parse (`-Werror`) |
| Golden previews | `cinder-host -- --check` | 234 pixel hashes, all match |
| CI on `main` | GitHub Actions | green |

**This is a healthy tree.** The gates are real, they run, and they pass. What follows is not a
rebuttal of that — it is what the gates cannot see.

### A1. The verification gap is still inverted, but much less than SHORTCOMINGS.md says

`SHORTCOMINGS.md` §A1 (2026-08-23) says 41% of the tree is "never compiled by any automated gate."
That is now **out of date in the project's favour** and should be amended rather than cited:

| Surface | Lines (tracked) | CI coverage today |
|---|---:|---|
| Rust | 47,975 | compiled + 549 tests |
| C / C++ | 30,509 | `-fsyntax-only -Werror`, 9 self-tests, 41 harness scenarios booting `main.cpp` |
| Shell | 6,807 | `shellcheck` (pinned) + `bash -n` + 55-case launcher matrix |

The honest residual claim is narrower and still worth making: **nothing in CI links or executes an
ARM build.** `host_syntax_check.sh` is `-fsyntax-only` — no codegen, no link, no ABI check. The
glibc-2.23 ceiling and the real cross-build live only in `build.sh`, which only `tools/release.sh`
calls. So a change that parses but does not link, or that raises the glibc floor, reaches a tag
before anything notices. `cinder-audio`'s IPC surface (the vtable offsets, argument shapes, object
sizes) remains untestable off-device, exactly as §A2 says.

### A2. `CLAUDE.md` is the most stale document in the repository, and it is the one read first

Its own banner is dated **2026-07-25** and states:

> *"Last code change was 2026-07-03 (tenth round); the tree is clean and the resume point is that
> device session."*
> *"The entire remaining critical path is device-gated and blocked on ONE thing."*

Since then: 53+ commits, v0.3.0 → v0.3.4 shipped, USB-DAC→LDAC executed end to end, palettes
device-verified, FM tuner, Bluetooth, NFC, playlists, keyboard, lyrics and search landed. The
file's "host tests green: 39 UI + 8 DB" is now 549.

This is **the identical failure the project already diagnosed and fixed for `ROADMAP.md`**
(`AUDIT_2026-09-01` §D2: *"A roadmap that is five weeks stale is worse than no roadmap, because
unlike a dated audit it is supposed to be current"*). `ROADMAP.md` got a banner; `CLAUDE.md` did
not, and `CLAUDE.md` is loaded into the context of every session automatically. Fixing this is the
single cheapest high-leverage action in this document.

### A3. 32 MB of stale preview PNGs are still committed, and `golden.txt` has made them redundant

`AUDIT_2026-09-06_ui.md` Part D recommended deleting these eight days ago. Verified still tracked:

| Directory | Files | Size |
|---|---:|---:|
| `player/cinder-host/out/` | (part of 432) | 17 MB |
| `player/cinder-ui/src/out/` | (part of 432) | 15 MB |
| **Total tracked** | **432** | **32 MB** |

Two divergent August snapshots of a UI that no longer exists, one of them inside a Rust `src/`
directory. The argument for keeping them was reviewability — but **`golden.txt` (234 hashes) now
does the regression job**, and it does it better, because it fails CI instead of waiting to be
looked at. Delete both, `.gitignore` them, publish the render as a CI artifact.

### A4. Branch protection is on — and the owner is exempt from it

`AUDIT_2026-09-01` §D5 called branch protection *"the most important item in this document"*.
**It is applied** (checked through the API, 2026-09-14): `main` requires all six CI checks, strictly
(the branch must be up to date), and refuses force pushes. What it does **not** do is apply to
admins (`enforce_admins: false`) or require a review — which is why every one of the last 14
commits is a direct push to `main` (CI event `push`) and still lands. CI is green on all of them, so
this is latent, not active, harm.

For anyone else the gate is already real: an outside pull request cannot merge red. The open
question is only whether the owner's own pushes should go through it too — one API call
(`POST /repos/superwilso/Cinder/branches/main/protection/enforce_admins`), and after it every change,
the owner's included, needs a pull request with green checks.

### A5. The highest-consequence unverified code in the tree is shipping to strangers right now

`installer/src/stage.rs` sends the 12-byte vendor SCSI command itself under `#[cfg(windows)]`
through `IOCTL_SCSI_PASS_THROUGH_DIRECT`. Per `STATUS.md` (2026-09-12) it is *"compiled but never
executed on Windows"* — and since Sony's updater was removed, **it is the only way a Windows user
can install anything.** It is live in v0.3.4, which is what a Reddit link points at.

Downloads as of this audit: **2 Windows, 1 Linux.** The window to test it before it meets an
audience is open and small. `DEVICE_CHECKLIST.md` 11.9 already describes the test.

> **Update, later on 2026-09-14 — owner-reported, no log captured:** the owner has run the
> **v0.3.3** installer. `installer/` is byte-identical between `v0.3.3` and `v0.3.4` apart from the
> version number (`git diff --stat v0.3.3 v0.3.4 -- installer/`: `Cargo.toml` and `Cargo.lock`, one
> line each), and the Windows pass-through landed before `v0.3.2`, so a v0.3.3 run on Windows
> exercises exactly the code v0.3.4 ships. If that run was the Windows executable, A5 is closed in
> substance; 11.9 still wants its install log attached to be recorded as a pass.

---

## Part B — What to do next, in order

> **Superseded for ordering by [Part E](#part-e--the-detailed-plan-after-v035)**, written the same
> evening once v0.3.5 had shipped. B0 #2 and B1 #5–7 are done; the rest is carried into Part E.

Ordered by *consequence × cheapness*, not by interest.

### B0. Now — before the Reddit thread brings anyone

| # | Action | Why now | Cost |
|---|---|---|---|
| 1 | **Record `DEVICE_CHECKLIST.md` 11.9** — the Windows installer, end to end, from the GitHub release | The only install path for Windows users. The owner has since run the v0.3.3 installer (A5, update); what is missing is the platform and the install log that make it a recorded pass | One device session |
| 2 | **Banner `CLAUDE.md`**, pointing at `STATUS.md` / `DEVICE_CHECKLIST.md` / this file | It is read first by every session and is seven weeks wrong (A2) | Minutes |
| 3 | **Decide whether branch protection binds the owner** (`enforce_admins`) | Protection is on and already gates outside PRs; the owner's direct pushes bypass it (A4). Owner's call — it turns every change into a pull request | One API call, needs the owner |
| 4 | **Point the Reddit post at GitHub Issues** | Templates already exist (`bug_report`, `device_report`, `feature_request`) and route correctly. It is also the only channel a cloud session can watch (D1) | One post edit |

### B1. Next — cheap, and each one removes a whole class of future pain

| # | Action | Why |
|---|---|---|
| 5 | **Delete both `out/` directories**, `.gitignore` them, publish previews as a CI artifact | 32 MB of misleading August pixels; `golden.txt` already covers regressions (A3) |
| 6 | **Add a linking ARM build to CI** (or state plainly that `release.sh` is the only real build gate) | `-fsyntax-only` cannot catch a link error or a glibc-floor rise (A1) |
| 7 | **Amend `SHORTCOMINGS.md` §A1** with today's coverage table | It is cited by ID elsewhere; leaving it overstating the gap devalues the citations |
| 8 | **Finish the `scale.rs` migration** — the caption tier (10/11/12 px) and the 13/14/15 row subtitles | See Part C: this is a **prerequisite** for skins, not a cosmetic tidy-up |

### B2. The device backlog — unchanged, still the critical path for truth

`DEVICE_CHECKLIST.md` holds it. Open and highest-value: **11.9** (installer, above), **5.1**
(volume limit caps), **5.4** (DSEE HX is not another high gain — a null result matters),
**5.5** (any battery discharge number at all; every sample ever taken was on the cable),
**11.3** (a quiet boot never falls through to stock).

**11.10 (USB-DAC → LDAC) passed but is owner-reported with no log.** Re-running it with
`adb shell 'tail -f /contents/cinderhome.log'` costs five minutes and upgrades the project's
headline claim from a sentence to an artefact. It is the best value-per-minute item on the list.

### B3. Then — the design system

Part C. It is deliberately *after* B0–B2: it is a large refactor of the app's brain, and doing it
while the installer is unverified would be building on a floor nobody has stood on.

---

## Part C — The skins design system

**Decision recorded 2026-09-14: skins only.** Data-driven layouts (JSON/Lua/WASM) are out of
scope — `PLAN_skins.md`'s "Not recommended" section stands, and the owner has confirmed it. A skin
is Rust, compiled in, chosen in Settings.

This part **revises** `PLAN_skins.md` step 2. The goal, the three layers and the ordering are kept.
One thing in the stated contract is wrong, and it would break 229 tests on contact.

### C1. Where it starts from, measured today

| | 2026-09-11 (`PLAN_skins.md`) | **2026-09-14 (`7860396`)** |
|---|---|---|
| `nav.rs` total | ~13,000 | **13,267** (7,758 logic + 5,509 tests) |
| Coordinate-driven interactions in its tests | 213 | **229** |
| Screen modules | — | **44** |
| Screens answering taps with a named `Hit` | 8 | **6 modules** (`confirm`, `bluetooth`, `fm`, `library`, `pairing`, `shelf`) |
| Screens handed a `View` struct | — | **3** (`QueueView`, `SettingsView`, `DeviceView`) |
| Distinct font sizes at call sites | 314 literals | **24 distinct sizes**, `scale.rs` exists but most sites bypass it |

The shape of the problem is unchanged and the tree has moved *toward* the plan (`scale.rs` landed;
`golden.txt` landed). Two structural facts dominate everything below: **`nav.rs` is the only thing
that knows what a tap means**, and **layout is answered by 70+ small functions scattered across the
screen modules**.

### C2. The correction: layout is a pure function, not a side effect of painting

`PLAN_skins.md` specifies the hit map as populated *while drawing*:

> ```rust
> /// Draw one frame of `view`, registering everything tappable as it is drawn:
> ///     ui.hit(rect, Hit::Play);
> ```

**This cannot work on this codebase, and the evidence is decisive.** Of the 229 coordinate-driven
interactions in `nav.rs`'s tests, **exactly 5 call `render()` first.** Every other test constructs
an `App` and taps it without ever painting. Under "register as it is drawn," all of them would look
up a tap in an empty hit map.

The problem is not the tests — it is what the tests reveal about the design. Making hit-testing a
side effect of painting means:

* a tap before the first paint has no defined answer;
* a tap arriving after a state change but before the repaint is tested against **the previous
  frame's** geometry;
* hit behaviour becomes dependent on *paint order* and on clipping — a control under a clip or
  drawn off-panel silently stops being tappable;
* the headless suite, which is this project's fastest and most valuable gate, cannot run at all.

That last one is fatal. The 229 tests are the thing that lets a 13,000-line god object be
refactored safely.

**The fix keeps the actual goal.** The goal was never "register while drawing" — it was
*render and hit-test must not be able to disagree*, which is what killed the Menu chevron, the
Folders bar and the picker scrollbar. Single-sourcing achieves that; paint-time registration is
just one way to get it, and the wrong one here.

```rust
pub trait Skin {
    fn name(&self) -> &'static str;

    /// Resolve geometry for `view`. PURE: no Canvas, no side effects, no paint order.
    /// Both `render` and `App::tap` call this, so they cannot disagree.
    fn layout(&self, view: &View) -> LayoutMap;

    /// Draw, consuming the SAME LayoutMap the hit test will use.
    fn render(&self, c: &mut Canvas, t: &Theme, f: &FontSet, view: &View, l: &LayoutMap);
}

pub struct LayoutMap {
    regions: Vec<(Rect, Hit)>,   // named targets, in hit-priority order
    sliders: Vec<(Rect, SliderId)>,
    content_h: i32,              // for fling/clamp/scrollbar, owned by App
}
```

Consequences, all good:

* `App::tap` becomes `skin.layout(&view).hit(x, y)` → `App::on_hit(Hit)`. **No coordinates in
  `nav.rs`.**
* The 229 tests keep working unchanged — they tap an `App`, which resolves layout on demand.
* `LayoutMap` is *testable on its own*, which is what makes the contract suite in C4 possible: you
  can assert "every region ≥ 44 px, none overlapping" without rendering a pixel.
* Layout can be cached per `(screen, view-revision)` — the bench below says it needn't be, but the
  option exists and paint-time registration forecloses it.

**Cost is not a concern, and it is measured.** From `render_bench` on this tree (host x86; the
device is far slower, but both sides scale together so the *ratio* holds):

| | |
|---|---:|
| `up_next::layout` | **5.8 µs** |
| `up_next::render_view` | **249.4 µs** |
| `albums_build` | 4.2 µs |
| `library albums` (render) | 224.2 µs |
| `just canvas fill` (a blank screen) | 42.6 µs |
| Now Playing, cover page | **4,473 µs** |

Layout is **2.3%** of the frame it belongs to, and a blank canvas fill costs seven times more than
resolving Up Next's entire list geometry. Resolving layout on demand — even twice a frame — is not
where this app spends time. Cover art is.

### C3. Revised order

Step 0 (`golden.txt`) and step 1 (palettes) are **done and device-verified**. The rest:

| Step | What | Gate that proves it |
|---|---|---|
| **2a** | **Finish `scale.rs` migration** (B1 #8). Collapse the caption tier and row subtitles onto named roles | `golden.txt` re-blessed deliberately, with the 31-screen overflow matrix re-baselined **against rendered PNGs** — the caption tier *grows* on collapse, which is why it was deferred |
| **2b** | **`View`** — one enum over per-screen view structs. 3 of ~31 screens have one; the rest still take positional args | Compiles; `golden.txt` must not move |
| **2c** | **`LayoutMap` + `Hit`**, pure `layout()`. Migrate status bar, Now Playing and Menu first — smallest surface, and it retires the literal transport circles (`hit(240, 692, 44)`) | `golden.txt` unmoved **and** all 229 coordinate tests still green |
| **3** | The remaining screens, **Library last** (~300 references from `nav.rs`) | Same two gates, per screen |
| **4** | Extract today's screens as the `cinder` skin; add `--skin` to `cinder-host` and `cinder-sim` | A second skin renders without touching `nav.rs` |
| **5** | **First new skin — Terminal**, the furthest from Cinder, so it tests the contract hardest. `design/handoff_raw/nw-a55/project/screens-terminal*.jsx` already holds the design | The contract suite (C4) passes for it |

Steps 2a–2c are the whole risk. After them, each screen is mechanical and independently shippable.

### C4. The contract suite — what makes it a standard

One suite, run against every registered skin. Passing it is what being a skin *means*. Most of
these are now cheap because `LayoutMap` is inspectable without painting:

* nothing drawn off-panel (`tests/ui_overflow.rs` already does this for one skin — 31 screens × 2
  content sets × 2 themes × 7 UI scales);
* **every hit region ≥ 44 px, and no two overlapping** — a pure `LayoutMap` assertion;
* **every screen has a reachable way back** — the rule that would have caught the inert Menu
  chevron before a human did;
* **a screen whose list stops at `library::list_bottom()` draws the Now Playing bar** — the
  Folders/Up Next defect, already a rule, now enforced per skin;
* no panic on an empty library, hostile strings, night mode, or 140% UI scale;
* every palette that loads still meets its readability floors on the skin's surfaces;
* a per-skin `golden.txt`, so a skin's own changes are reviewed as pixels.

### C5. Safety — one gap worth closing

`PLAN_skins.md` notes a skin cannot touch the device, *"but a panic still ends the process, and the
device reboots into the escape ladder."* That is true and it is a poor outcome: **falling back to
Sony's firmware because a skin had a bad number** is a brutal failure mode for a cosmetic feature.

The project already owns the right pattern — the bad-boot counter. Apply it one level up: if the
app dies twice with skin *X* selected, revert to `cinder`, log why, and say so in Settings. An
unknown `skin=` in the settings file already falls back to `cinder` (the rule `Accent::from_index`
and the palette loader both follow). This extends that from "unknown" to "unsurvivable."

---

## Part D — Maintainer watch

**Decisions, 2026-09-14:** fixes go to a **draft PR** and never straight to `main`; **any** new
issue or bug report pings the owner; **GitHub is the channel** (a cloud session cannot read Reddit —
see below); device work happens by **script-and-paste**.

### D1. Reddit cannot be read from the cloud build environment — it can from the owner's machine

In the cloud session that wrote this, tested four ways: `WebFetch`, `curl https://www.reddit.com/...`,
`old.reddit.com`, and the `.json` API. All four fail identically at the proxy with
**`connect_rejected` — 403 to CONNECT (organization policy)**: an egress allowlist denying the
domain, which RSS shares. No workaround was attempted there, deliberately.

**That is a property of that environment, not of Reddit.** On the owner's own machine the thread's
Atom feed (`…/comments/1wedu23/.rss?sort=new&limit=100`, browser User-Agent) returns 200, and a
local session has been watching it alongside GitHub on 2026-09-13/14. The `.json` endpoints do 403
there too, and WebFetch is blocked for the domain, so the feed is the one route.

So the conclusion stands for anything **scheduled in the cloud**: funnel reports to GitHub Issues,
where the templates already exist and already route bricks/boot failures to `RECOVERY.md` and
`SECURITY.md`. A local session can keep reading the thread directly.

### D2. What the watch does

An hourly Routine wakes a session that:

1. lists new/updated issues, issue comments, PRs and PR review comments on `superwilso/Cinder`,
   ignoring anything authored by the owner or a bot;
2. for anything new: reproduces it if it can be reproduced off-device, writes the fix, runs
   **all** gates (549 tests, 41 harness scenarios, syntax check, `golden.txt`), pushes a branch and
   opens a **draft PR**;
3. **drafts** a reply with the diagnosis and the PR link, and hands it to the owner — it does not
   post it. Anything said in public on the project is in the owner's own words;
4. **pings the owner's phone** — on every new report, per D's decision, whether or not a fix landed,
   with the draft reply attached;
5. if it is device-gated, writes a self-contained diagnostic script, says so, and does not guess.

**The boundary it does not cross:** nothing merges to `main`; nothing is posted publicly in the
owner's name; nothing is pushed that fails a gate;
anything touching the boot path, the installer, root helpers or Sony IPC gets a diagnosis and a
proposed patch rather than a speculative fix, because CI cannot execute that half of the tree
(A1) and a wrong guess there costs a device with no public recovery path.

---

## Part E — The detailed plan, after v0.3.5

*Written 2026-09-14, evening, against `3ad5a14` (v0.3.5). Every status below was checked that
evening; estimates are marked as estimates.*

### E0. Where things stand

| | |
|---|---|
| **Shipped** | v0.3.5, published 16:49 UTC: bring-your-own WM1A/W1 volume tables, build attestation, the setuid-count and Bluetooth-address corrections, README screenshots re-rendered by the release script |
| **Provenance** | Proven, not assumed: GitHub's attestations API returns one SLSA v1 provenance record for the v0.3.5 Linux installer, naming `.github/workflows/release.yml` at `refs/tags/v0.3.5`. (`gh attestation verify` needs GitHub CLI 2.49 or later; 2.45, Ubuntu's package, does not have the command.) |
| **CI on `main`** | **Red on one step.** `tools/check_arm_payload.sh` was committed without its executable bit (`core.filemode=false` in the maintainer's clone), so the step exits 126 before checking anything. The fix records the bit and calls the script through `bash` |
| **Downloads** | v0.3.3: 4 Windows, 1 Linux, 1 `.upg`. v0.3.4: 3 Windows, 1 Linux. v0.3.5: none yet |
| **People** | One active tester on Reddit with a real library and real problems; eight feature requests (E4); no GitHub issues yet |
| **Device-verified today** | The WM1A curve from Wampy's tables through the install step, `volume curve: wm1a applied` at boot, PHV 84 at vol 40 and 124 at vol 60 |

### E1. Track 1 — support and release hygiene (now)

#### P0. Every install or update through the installer ends on Sony's player

The one active tester reported, the morning after updating: *"Seems like it refuses to switch into
your bootloader now. Any way to force that without a PC?"* The code explains it without needing
their log:

1. The installer tells the user, three times, that the player *"reboots into its own updater …
   comes back on its own. Do not unplug it."* (`installer/src/gui.rs`).
2. The launcher's first check — rung 0, the cable escape — sends **any boot with USB connected**
   to stock: `usb_connected` accepts the gadget state `CONFIGURED`/`CONNECTED` or `usb/online`,
   re-checks after 3 s, and runs Sony's player (`install_cinderhome.sh`, the launcher heredoc).
3. So the updated player restarts with the cable in, exactly as told, and lands on Sony's UI. Unless
   the cable was pulled in the few seconds between power-on and the launcher, **the install looks
   like it failed.** It did not fail; the escape did its job on a boot nobody meant it for.

Checklist 11.9's pass line says "boots into Cinder" without saying where the cable is, which is how
this got through.

| option | what it does | verdict |
|---|---|---|
| **(a) Say it** | The installer's last message becomes: *when the player shows Sony's screen after updating, unplug the cable and restart it — a cable at power-on always starts Sony's player, which is the recovery escape.* The window's final state names it too: "Installed — unplug and restart the player to start Cinder" | **Do this.** No boot-path change, no escape weakened, one release |
| (b) Exempt the first boot after an install | A one-shot flag the install writes and the launcher consumes | **No.** The first boot of a new build is when the escape matters most; it breaks rule 4 in spirit (an escape must depend on less than what it rescues) |
| (c) Narrow rung 0 to a PC data connection | Ignore chargers, keep PCs | Does not help here — a PC *is* connected. It is 11.4's decision, taken after 11.2/11.3 |

**Decision (owner, 2026-09-14): a one-shot pass, built the way (b)'s objection asks.** The verdict
above rejected (b) because the first boot of a new build is when the escape matters most. The pass
answers that in the launcher, not the app: `install_cinderhome.sh` writes
`/data/cinder/cable_pass_once` after its sanity gate, and the launcher **deletes it before honouring
it**, on every boot, cable or not, and honours it only if the delete worked. So it covers one boot
whether or not the new build ever runs: a build that hangs on that boot costs one more restart, and
the restart escapes. POWER in the logo and the bad-boot counter stay armed through it, cinder-home
removes a leftover pass once healthy, and it lives on `/data` only, so nothing can leave it set over
USB-MSC. `tools/cinder-install.sh` uses the same pass instead of borrowing `cable_escape_off`.
Option (a)'s message is no longer needed; the installer now says the first start ignores the cable
and later ones do not. Matrix: `cinder-home/tools/test_launcher.sh`.

**Acceptance (revised for the decision):** an Update with the cable in boots Cinder; a restart with
the cable still in lands on Sony's player; the same round trip is recorded as 11.9's pass, with the
cable state written into the row. **The facts for the reply** (the owner's words, not these): unplug
the cable, hold POWER to switch off, power on without it. If Sony's player still comes up, the two
logs that settle it are `cinder_home_install.log` in the drive root and `cinderhome.log`.

#### The rest of Track 1

| # | Action | Acceptance |
|---|---|---|
| 1 | ~~Commit the executable-bit fix~~ **Done** (`7f1e841`, `0a5a172`) | CI green on `main`; the `ui-previews` artifact is on the run — both true of run 34897603508 |
| 2 | ~~Release notes and `SECURITY.md`: add "GitHub CLI 2.49 or later" beside `gh attestation verify`~~ **Done 2026-09-14** | A reader on Ubuntu's `gh` is not told to run a command they do not have |
| 3 | Publish the `wm1a-volume-tables` draft on cinder-sony-analysis (read its wording first) | The install.md link resolves |
| 4 | Record 11.9 and 13.9 (b)/(c) with logs and the cable state | Rows read PASS with dates |
| 5 | The community watcher records everything it finds on its first pass as "seen", which is how the P0 report went unflagged for four hours. Start it with a 24-hour look-back | A report older than the watcher still reaches the owner once |

### E2. Track 2 — device sessions (the owner, with the player)

Grouped so each session needs one setup. The pass criteria are the checklist's; this is the order.

| Session | Items | Setup | Time (est.) |
|---|---|---|---|
| **A. Installer, on Windows** | P0 acceptance, 11.9 end to end (Install → Update → Uninstall → Install), 13.9 (b) and (c) | Windows, the release `.exe`, the cable, a card-less player | 1 h |
| **B. What the release added** | 13.4 Storage row, 13.7 lyrics (the two named songs, then a `.lrc`), 13.8 search, 11.12 palettes, 11.11 read the label | Nothing special | 45 min |
| **C. The headline feature, with evidence** | 11.10 USB-DAC → LDAC with `adb shell 'tail -f /contents/cinderhome.log'` running; look for `-EBUSY` (Sony's `UsbDeviceAudioPlayerService` holding the capture PCM), the UAC switch itself, and the 3.5 mm leg with no Bluetooth link | PC as audio source, LDAC headphones | 30 min |
| **D. Boot robustness** | 11.3 five quiet boots, then **11.4 — the cable escape decision**, now informed by the P0 | Cable out | 30 min, then a decision |
| **E. Battery** | 5.5 a discharge number (`tools/battery_track.sh start 60`, unplug, use it for hours), 13.1 the flat-battery shutdown | A day off the cable | a day, mostly waiting |
| **F. Measurements** | 5.1 volume limit caps at 63, 5.4 DSEE null test on a lossy file, 13.6 `_cew` at the jack | The jack rig (`RE_headphone_amp_modes.md`), nothing on a head | 1–2 h |
| **G. Opportunistic** | 5.2 fine-volume stutter, 5.3 `MIN` not `MUTE`, 11.7 NEW PLAYLIST band, 11.8 power after a guard recovery | During normal use | — |

Sessions A–C are worth doing before v0.3.6; D decides whether v0.3.6 also changes the launcher.

### E3. Track 3 — skins, as pull requests

Part C's contract, cut into steps that each leave `main` releasable. **Every step's gates: all
host tests (552 `#[test]` functions today), `golden.txt` unmoved unless the step says otherwise, the
coordinate-driven tap tests green, pre-push, CI.** `nav.rs` is 13,267 lines; keep each PR reviewable
(an estimate: under ~800 changed lines outside tests).

| Step | PR(s) | What changes | Special gate | Size (est.) |
|---|---|---|---|---|
| **2a** | 1–2 | The `scale.rs` migration: captions (10/11/12 px) and row subtitles (13/14/15) onto named roles | `golden.txt` **re-blessed deliberately**; the 31-screen overflow matrix re-baselined against the rendered PNGs (the caption tier grows when it collapses — why it was deferred). README screenshots re-render at the next release on their own | 1–2 sessions |
| **2b** | one per screen family (Now Playing, Queue, Library, Settings, radio/BT/DAC, overlays) | A `View` enum over per-screen view structs; 3 of ~31 screens have one today | Pixels unmoved | 4–6 sessions |
| **2c** | 1 | `LayoutMap` + `Hit` + a pure `layout()`, used first by the status bar, Now Playing and Menu; retires the literal transport circles | Pixels unmoved **and** every tap test green | 2 sessions |
| **3** | one per family, Library last (~300 references from `nav.rs`) | Remaining screens answer taps through `LayoutMap` | Same | 5–8 sessions |
| **4** | 1 | Today's screens become the `cinder` skin; `--skin` in `cinder-host` and `cinder-sim` | A second skin renders without touching `nav.rs` | 1–2 sessions |
| **5** | 2 | The contract suite (C4), the "dies twice → back to `cinder`" fallback (C5), then the Terminal skin (design already in `design/handoff_raw/`) | The suite passes for both skins; a skin that panics twice is reverted and says so in Settings | 3–4 sessions |

**What users see:** nothing until step 5, which is the point — 2a–4 are refactors behind golden
pixels. The "install any UI" idea from the Reddit thread is step 5 plus a skin picker; a *marketplace*
of downloadable skins is not in this plan, because skins are compiled Rust (Part C, "skins only").
Palettes already cover downloadable colour schemes.

### E4. Track 4 — features: the owner's goals and the community's requests

**The ten goals (2026-06-24), where each stands:**

| # | Goal | Status | Next |
|---|---|---|---|
| 1 | Faster boot, better battery | Idle power work landed; **no discharge number exists** | Session E |
| 2 | Improved UI/UX | Cinder UI shipped; palettes, type scale | Track 3 |
| 3 | USB-DAC with LDAC *and* 3.5 mm | Implemented in cinder-home; control plane proven; audio owner-reported, no log | Session C |
| 4 | Night mode that dims the screen | Done | — |
| 5 | Built-in scrobbler | Done (`.scrobbler.log`) | — |
| 6 | Queue and shelf | Done | — |
| 7 | All Sony effects, and on Bluetooth | 25 of Sony's effect setters are wired, ClearAudio+, Dynamic Normalizer, VPT modes, DC Phase, Tone Control and Source Direct among them. **Whether any reach the LDAC path is unmeasured** | RE + measure: `--dseemeter` with a BT link up |
| 8 | Keep the built-in sound card | Done by design | — |
| 9 | Lock screen = keylock | Done | — |
| 10 | 2038 | Userland not audited; the kernel limit is documented | An i64 audit of Cinder's own timestamps (scrobbler, likes, playlists, library) |

**The Reddit thread's requests:**

| Request | Status | Next |
|---|---|---|
| Choose the accent colour | **Exists** — Settings ▸ Display, six accents | Reply only |
| Search | Shipped in 0.3.4, songs only | Albums and artists as results |
| Lyrics | Shipped in 0.3.4: `.lrc`, FLAC, MP3 `USLT`, M4A | 13.7; later `SYLT`, Ogg, DSF |
| SD card library not found (1 TB, player-formatted) | Fixes in 0.3.4 | 13.5 needs the reporter's log |
| Don't scan the database at startup | **Exists** — scan on demand from Settings ▸ Database | Reply only |
| "Add to queue" / "Play next" | **Exists** as a swipe, and the asker did not know | Discoverability: name it in onboarding, and offer it on long-press as well as the swipe |
| Install any UI / theme marketplace | Palettes exist; skins are Track 3 | Step 5 |
| Run on top of Walkman One | Untested; Walkman One changes the updater key, so stock-keyed packages are refused | Needs someone with Walkman One installed. The installer could detect the key mismatch and say so |
| Clear Bass+ from the NW-ZX100 | **Not in this firmware** — no Clear Bass symbol or label in the A50's DSP libraries or catalogues | Say so; a bass preset through Tone Control or the EQ is possible |
| SensMe channels | Data-gated: only files analysed by Sony's PC software carry the tags | Needs a tester with such a library; the database rows are readable without further RE |
| NW-A40 series | Not supported (different firmware) | Reply only |
| Big playlists (a 7,000-song MusicBee playlist) | `.m3u8` in `cinder_playlists` is read; nobody has measured a big one | A host benchmark with a synthetic 10,000-line playlist before promising it |

**Also open from `STATUS.md`:** discovering an *unpaired* Bluetooth device (pairing is otherwise
real), and the BT Receiver screen, which is static and says so — decide to build it or remove it.

### E5. Track 5 — project hygiene

| Item | Why | Size (est.) |
|---|---|---|
| **`cargo fmt --check` gate** (SHORTCOMINGS Part F #9) | `fmt` fails both workspaces today; one mechanical formatting PR per workspace, then the gate. Best done **before** Track 3, whose diffs would otherwise drown in reformatting | 1 session |
| **Tests for `cinder-audio`** (A2) | Zero today, and it drives every Sony service. Grow `fake_pst.cpp` one service at a time, starting where a wrong argument shape once rebooted the device (BtTransmitter) | ongoing, a scenario at a time |
| **Windows smoke per release** | P0 shows the installer's end state is where users judge it. Session A's round trip, every release, with the log attached | 1 h per release |
| **Code signing** (D7, still open) | SmartScreen conditions users to click through. Free signing for open-source projects exists (SignPath's OSS programme is one); eligibility is worth checking before paying for a certificate | investigation |
| **`enforce_admins`** | Owner's decision (A4) | one API call |
| **History rewrite** | Owner's decision; runbook and redaction step ready (`docs/HISTORY_REWRITE.md`) | an afternoon, irreversible |

### E6. Milestones

| Release | Contents | Gate |
|---|---|---|
| **v0.3.6 — support** | P0: the one-shot post-install cable pass (decided 2026-09-14); CI green; release-notes CLI version; watcher look-back. Possibly 11.4's launcher decision if Session D is done | Sessions A–C recorded |
| **v0.4.0 — foundations** | `cargo fmt` gate; skins 2a (the only visible change: caption sizes); 2b–2c behind unmoved pixels; Play Next discoverability; search results for albums/artists | 11.10 recorded with a log; battery number from Session E |
| **v0.5.0 — skins** | Steps 3–5: the `cinder` skin, the contract suite, the fallback, the Terminal skin and a picker in Settings | The contract suite passes for both skins on the device |

### E7. Decisions only the owner can make

1. ~~**P0:** option (a)~~ **Decided 2026-09-14 (owner): a one-shot post-install pass** — see the decision note under P0. Still open: whether the installer should also detect "came back on Sony's player".
2. **11.4, after Session D:** keep rung 0 as is, narrow it to a PC connection, or remove it.
3. **`enforce_admins`** on `main`.
4. **The history rewrite**, and whether to ask GitHub Support to purge cached views after it.
5. **BT Receiver:** build or remove.
6. **Code signing:** free programme, paid certificate, or stay unsigned.

### E8. Deferred past v0.3.6: generated files keep themselves current, and a nightly build

*Asked for 2026-09-14, then deliberately parked until v0.3.6 is out.* The owner's decisions: after
every push to `main`, CI regenerates what goes stale when someone forgets it (the README's app
previews and installer screenshots first), a bot commits the result **straight to `main`**, and the
push also refreshes a **`nightly` pre-release**.

| # | Step | Notes |
|---|---|---|
| 1 | `refresh.yml`, job `app-shots` on ubuntu: `tools/render_screenshots.sh` | The previews are pixel-deterministic (`golden.txt` already proves that across machines), so a runner renders the same bytes a maintainer does |
| 2 | job `installer-shots` on windows-latest: `bash tools/render_installer_screenshots.sh` | The script needs a native-Windows branch: Git Bash has `cygpath -w`, not `wslpath`, and the default MSVC target instead of `x86_64-pc-windows-gnu`. Capture already falls back from DWM to `WM_PRINT`; whether a runner session composes at all is unproven, so look at the first run's images |
| 3 | job `commit`: fetch both artefacts, copy into `docs/screenshots`, commit only if something changed | Once CI renders the installer screenshots, **remove `release.sh` step 3c**: the runner's Windows and the maintainer's draw different pixels and would take turns overwriting each other |
| 4 | **The blocker: branch protection.** `main` requires six status checks, strict. A push made with `GITHUB_TOKEN` cannot satisfy that, so the bot's commit is refused | Pick one: a ruleset bypass for GitHub Actions scoped to generated paths; a fine-grained token with bypass, kept as a secret; or push to a `refresh` branch and auto-merge its PR once checks pass. `GITHUB_TOKEN` pushes start no workflows, so there is no loop, and also no CI on the bot's commit, which is acceptable only while it touches generated files alone |
| 5 | `nightly.yml`: `release.yml`'s two installer builds on every push to `main`, then move the `nightly` tag and replace the pre-release's assets, attested the same way | **The caveat its notes must print:** the installer embeds the committed `cinder-home/dist/stable`, which only `tools/release.sh` rebuilds. Player changes (the 2026-09-14 Library sort, for one) are **not** in a nightly until a release has rebuilt dist/. Name the payload manifest's tag in the notes. Moving a tag is not a branch force-push, so `allow_force_pushes: false` on `main` does not stand in its way |
| 6 | Later, if nightlies should carry player changes | Build dist/ on a runner: the glibc-2.23 + libc++-3.9.0 cross toolchain in a container image. The largest piece, and it would also let `release.yml` stop trusting a committed payload |
| 7 | Other things that go stale when forgotten | Candidates: the test and preview counts in `CLAUDE.md`'s banner, and `docs/README.md`'s index. Each needs a generator before CI can refresh it |
